{% if app.session.get('_app.stores') is iterable %}
    {{app.session.get('_app.stores')[app.session.get('_app.store')]['name']}}&nbsp;<b class="caret"></b>
{% else %}
    <a href="#"
       data-toggle="popover"
       data-placement="bottom"
       data-html="true"
       data-content='<span class="lnh-lg">Starts by creating your store <a href="{{ path('_bck__admin__store__index') }}" class="label">Stores</a>, then <a href="{{ path('_login__default__process') }}" class="label">Refresh</a> application.</span>'
       class="badge badge-danger">No store available!</a>
{% endif %}