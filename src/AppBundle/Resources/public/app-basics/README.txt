Basic default features for any action.
We use the "default" action in directory tree to simulate a regular action directory tree, so we can set relative
paths (like font) in the way to be compatible with any default action structure.